"""Shared helper package for the Rocky Top Outfitters pipeline.

Modules (added step by step):
    config  -- non-secret paths, URLs, table/column names
    db      -- SQLAlchemy engine factories (UTK MySQL + local SQLite)
"""
